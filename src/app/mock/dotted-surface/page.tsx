import DottedSurfaceDemo from "./demo";

export const metadata = {
  title: "Dotted Surface · Mock",
  robots: { index: false, follow: false },
};

export default function DottedSurfaceMockPage() {
  return <DottedSurfaceDemo />;
}
